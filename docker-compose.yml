version: '3.8'

services:
  bot:
    build: .
    container_name: kurup-ads-bot
    restart: unless-stopped
    command: python -m app.bot.main
    env_file: .env
    volumes:
      - ./logs:/app/logs
      - ./assets:/app/assets

